import './Header.css'

/**
 * A React component that renders the header of the media viewer.
 *
 * @return {JSX.Element} The rendered header component.
 */
const Header = () => {
  return <div className="main">Imago media viewer</div>
}

export default Header
